from setuptools import setup, find_packages

setup(
    name='charts',
    version='1.6.3',
    description='Charts App',
    author='Your Name',
    packages=find_packages(),
    include_package_data=True,
    install_requires=[],
)
